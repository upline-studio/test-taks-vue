### **4.1.2 Именование классов**

Используйте осмысленные имена классов.

Мы используем именования классов используемые в БЭМ. Если вы не знакомы с этой методолгией то прежде чем продолжить чтение вам следует ознакомиться с двумя статьями:

[Быстрый старт БЭМ](https://wiki.yandex.ru/homepage/materialy/stati/bystryjj-start-bjem/)

[**Определения БЭМ**](https://wiki.yandex.ru/homepage/materialy/stati/opredelenija-bjem/)

**Соглашение по именованию**

Имя БЭМ-сущности уникально. Во всех технологиях (CSS, JavaScript, HTML) одна и та же БЭМ-сущность всегда называется одинаково. Основная идея соглашения по именованию — вложить смысл в имена и сделать их максимально информативными для разработчика.

Можно сравнить одно и тоже имя CSS-селектора, написанное разными способами:

- `menuitemvisible`
- `menu-item-visible`
- `menuItemVisible`

Чтобы понять смысл первого имени, нужно вчитаться в каждое слово. В последних двух примерах имя явно разделяется на логические части. Но ни одно из имен пока не помогает точно определить, что `menu` — это блок, `item` — элемент, а `visible` — модификатор. Чтобы имена сущностей были однозначными и понятными, в БЭМ были разработаны правила формирования имен БЭМ-сущностей.

**Правила формирования имен**

`block-name__elem-name_mod-name_mod-val`

- Имена записываются латиницей в нижнем регистре.
- Для разделения слов в именах используется дефис (``).
- Имя блока задает [пространство имен](https://ru.wikipedia.org/wiki/%D0%9F%D1%80%D0%BE%D1%81%D1%82%D1%80%D0%B0%D0%BD%D1%81%D1%82%D0%B2%D0%BE_%D0%B8%D0%BC%D1%91%D0%BD) для его элементов и модификаторов.
- Имя элемента отделяется от имени блока двумя подчеркиваниями (`__`).
- Имя модификатора отделяется от имени блока или элемента одним подчеркиванием (`_`).
- Значение модификатора отделяется от имени модификатора одним подчеркиванием (`_`).
- Значение булевых модификаторов в имени не указывается.

<aside> 📖 Важно! В методологии БЭМ не существует элементов элементов. Правила именования запрещают создавать элементы элементов, но в DOM-дереве элементы можно вкладывать друг в друга.

</aside>

Мы собираем классы SCSS-ом. Под 1 блок создается 1 файл. В файле мы открывает блоки и в нем реализуем весь код стилей относящихся к этому блоку в том числе и `@media`.

```scss
/* Не рекомендуется */
.component {
  &__child-1 {
    &--child-2 { // не надо так
      //
    }

    &__child-2 { // не надо так
      //
    }

    &anything { // не надо так
      //
    }

    // Возможен только один уровень вложенности блока
    // Допустимо добавить блок .component__child-1-child-2, но
    // это скорее исключение в редких случаях.
  }

  &__child {
    &-1 {
    }

    // так тоже не надо
    &-2 {
    }
  }

  // для каждого блока своё пространство 
  // &__child-1 {}
  // &__child-2 {}
}

.home { // home - один файл
  &-slider { // home-slider - другой файл
  }
}
```

```scss
/* Рекомендуется */
.component {
// styles

	// Добавление модификатора
	&_blue {}
	
	// Блок, зависимый от модификатора
	&_blue & { // это допустимо, но ...
		&__child-2 {}
	}

	&_blue &__child-2 { // ... но лучше так, особенно если мало елементов
	}
	
	// сложные модификаторы
	&_type {
		&_button { 
		}
		&_link {
		}
	}

	&__child-1 {
		// some child-1 styles

		&_red {
			// some child-1 modificator styles
		}

		&.active {
			// допустимо
		}
	}

	&__child-2 {
		//
	}
	
	@media (min-width: 500px){
	  // block styles
	  
	  &__child {
	    // elem styles
	  }
	}
}
```

Больше про методологию БЭМ вы найдете тут   https://ru.bem.info/

###

# **4.2 Правила форматирования CSS**

### **4.2.1 Порядок деклараций**

Мы используем следующий порядок декларации CSS-свойств:

1. **Позиционирование** (Positioning)
2. **Оформление блока** (Box Model)
3. **Типографика** (Typography)
4. **Визуальные свойства** (Visual)
5. **Анимация** (Animation)
6. **Разное** (Misc)

Пример кода:

```css
.declaration-order {
/* Позиционирование */
position: absolute;
top: 0;
right: 0;
bottom: 0;
left: 0;
z-index: 10;

/* Оформление блока */
display: block;
float: right;
width: 100px;
height: 100px;
margin: 10px;
padding: 10px;

/* Типографика */
color: #888;
font: normal 16px Helvetica, sans-serif;
line-height: 1.3;
text-align: center;

/* Визуальные свойства */
background-color: #eee;
border: 1px solid #888;
border-radius: 4px;
opacity: 1;

/* Анимация */
transition: all 1s;

/* Разное */
user-select: none;
}
```

Больше о порядке декларации см тут https://www.npmjs.com/package/stylelint-config-rational-order.

Неверный порядок декларации подсвечивает настроенный в сборке Stylelint.
